#!/bin/bash
# Managed by dotfiles/install.sh — global prepare-commit-msg hook
# Chains into the local repo's prepare-commit-msg hook if one exists.

local_hook="$(git rev-parse --git-path hooks/prepare-commit-msg 2>/dev/null)"
if [[ -x "$local_hook" ]]; then
  "$local_hook" "$@"
  exit $?
fi
