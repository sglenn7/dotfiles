#!/bin/bash
# Managed by dotfiles/install.sh — global post-rewrite hook
# Chains into the local repo's post-rewrite hook if one exists.

local_hook="$(git rev-parse --git-path hooks/post-rewrite 2>/dev/null)"
if [[ -x "$local_hook" ]]; then
  "$local_hook" "$@"
  exit $?
fi
