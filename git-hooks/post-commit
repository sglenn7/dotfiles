#!/bin/bash
# Managed by dotfiles/install.sh — global post-commit hook
# Chains into the local repo's post-commit hook if one exists.

local_hook="$(git rev-parse --git-path hooks/post-commit 2>/dev/null)"
if [[ -x "$local_hook" ]]; then
  "$local_hook" "$@"
  exit $?
fi
