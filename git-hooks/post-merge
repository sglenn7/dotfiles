#!/bin/bash
# Managed by dotfiles/install.sh — global post-merge hook
# Chains into the local repo's post-merge hook if one exists.

local_hook="$(git rev-parse --git-path hooks/post-merge 2>/dev/null)"
if [[ -x "$local_hook" ]]; then
  "$local_hook" "$@"
  exit $?
fi
